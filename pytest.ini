[pytest]
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
addopts = -m "not live" --strict-markers
markers =
    live: touches a real external service - run deliberately, never in CI
filterwarnings =
    ignore::DeprecationWarning:playwright.*
